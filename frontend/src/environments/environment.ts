// Environment configuration for development
export const environment = {
  production: false,
  apiUrl: '/api', // Use relative URL for Docker
  apiTimeout: 30000, // 30 seconds
  enableDebugLogs: true
};
